[data]
    name = "Corey Oliver"
    email = "corey.jon.oliver@gmail.com"
    github_username = "coreyjonoliver"
    personal = true
    use_1password = true

{{ if eq .chezmoi.os "windows" -}}
[data.roles]
    ai = {{ promptBoolOnce . "roles.ai" "Enable AI role (Ollama, local LLMs)" | not | not }}
    developer = {{ promptBoolOnce . "roles.developer" "Enable Developer role" | not | not }}
    gaming = {{ promptBoolOnce . "roles.gaming" "Enable Gaming role" | not | not }}
{{ end -}}

[onepassword]
    command = "op"
    prompt = true