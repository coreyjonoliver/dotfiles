{{ if .use_1password -}}
#!/bin/bash
set -euo pipefail

cat << 'MANUAL_STEPS'

╔══════════════════════════════════════════════════════════════════╗
║              1Password — Manual Configuration Required          ║
╠══════════════════════════════════════════════════════════════════╣
║                                                                  ║
║  The following settings must be enabled in the 1Password app.    ║
║  These cannot be automated (protected by cryptographic signing). ║
║                                                                  ║
║  Open: 1Password > Settings (⌘,)                                ║
║                                                                  ║
║  ┌─ Settings > Developer ─────────────────────────────────────┐  ║
║  │                                                             │  ║
║  │  1. ☐ Show Developer Experience                             │  ║
║  │       → Enable this FIRST (reveals the options below)       │  ║
║  │                                                             │  ║
║  │  2. ☐ Set Up the SSH Agent                                  │  ║
║  │       → Lets 1Password manage your SSH keys                 │  ║
║  │                                                             │  ║
║  │  3. ☐ Display key names when authorizing connections        │  ║
║  │       → Shows which key is being used for each connection   │  ║
║  │                                                             │  ║
║  │  4. ☐ Check for developer credentials on disk               │  ║
║  │       → Watchtower alerts for leaked keys/tokens on disk    │  ║
║  │                                                             │  ║
║  │  5. ☐ Generate SSH config file with bookmarked hosts        │  ║
║  │       → Auto-creates ~/.ssh/1Password/config from bookmarks │  ║
║  │                                                             │  ║
║  └─────────────────────────────────────────────────────────────┘  ║
║                                                                  ║
║  ┌─ Settings > Security ──────────────────────────────────────┐  ║
║  │                                                             │  ║
║  │  6. ☐ Check for vulnerable passwords                        │  ║
║  │       → Watchtower checks against Pwned Passwords database  │  ║
║  │                                                             │  ║
║  └─────────────────────────────────────────────────────────────┘  ║
║                                                                  ║
║  After enabling, verify the SSH agent works:                     ║
║    $ ssh-add -l                                                  ║
║    (should list your 1Password SSH keys)                         ║
║                                                                  ║
╚══════════════════════════════════════════════════════════════════╝

MANUAL_STEPS

# Try to open 1Password to the developer settings
open "onepassword://settings/developer" 2>/dev/null || true

{{ end -}}
